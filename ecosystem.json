{
  apps : [
    {
      name      : "Web",
      script    : "server.js",
      env: {
        PORT: "8000"
      },
      env_production : {
        NODE_ENV: "production"
      }
    },
    {
      name      : "Listener",
      script    : "button.js"
    }
  ],
  deploy : {
    production : {
      user : "nmelo",
      host : "192.168.2.220",
      ref  : "origin/master",
      repo : "git@github.com:nmelo/home-automation.git",
      path : "/home/nmelo/automation",
      "post-deploy" : "npm install && sudo pm2 startOrRestart ecosystem.json --env production"
    }
  }
}
